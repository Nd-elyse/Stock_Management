# Base URL for the Laravel API. Left as /api by default so the Vite dev
# proxy (see vite.config.js) forwards it to VITE_BACKEND_ORIGIN.
VITE_API_BASE_URL=/api
VITE_BACKEND_ORIGIN=http://localhost:8000
